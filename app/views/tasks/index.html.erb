<!-- <h1>Here are ALL of the Tasks You Have to Complete: </h1> -->
<h1><%= @welcome_msg %></h1>
<% @tasks.each do |task| %>
  <article>
    <h2><%= link_to(task[:title], show_path(task[:id])) %></h2>
    <p>status: <%= task[:status] == "yes" ? "YES! GOLD STAR ⭐️" : "Nope! C'mon now - hop to it!" %></p>
  </article>
<% end %>
<p class="go-to-new"><%= link_to("+ Add Task", new_path) %></p>
